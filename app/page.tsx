"use client"

import App from "../src/App"

/**
 * Root route – renders the entire inventory management application.
 * Placed in `app/page.tsx` so Next-lite can find a default component.
 */
export default function HomePage() {
  return <App />
}
